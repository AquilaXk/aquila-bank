DB_HOST=localhost
DB_PORT=5432
DB_NAME=aquila_bank
DB_USERNAME=postgres
DB_PASSWORD=postgres
OUTBOX_POLLER_ADAPTIVE_ENABLED=true
OUTBOX_POLLER_MIN_BATCH_SIZE=5
OUTBOX_POLLER_MAX_ADAPTIVE_DELAY_MS=8000
NOTIFICATION_CHANNEL_PROVIDER_WORKER_ENABLED=false
NOTIFICATION_CHANNEL_PROVIDER_WORKER_BATCH_SIZE=10
NOTIFICATION_CHANNEL_PROVIDER_WORKER_MAX_RETRY_DELAY_SECONDS=60

# Production GitHub Environment bootstrap:
# - tracked template: tools/ops/production-github-environment.env.example
# - local values: tools/ops/production-github-environment.env (do not commit)
# - apply safe vars: tools/ops/bootstrap-production-github-environment.sh --env-file tools/ops/production-github-environment.env.example --vars-only
# - apply secrets after filling local values: tools/ops/bootstrap-production-github-environment.sh --env-file tools/ops/production-github-environment.env
